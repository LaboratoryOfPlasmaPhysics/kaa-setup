---
- name: symlink /usr/bin/python -> /usr/bin/python3
  hosts: "*"
  gather_facts: false
  tasks:
    - name: symlink /usr/bin/python -> /usr/bin/python3
      raw: |
        if [ -f /usr/bin/python3 ] && [ ! -f /usr/bin/python ]; then
          ln --symbolic /usr/bin/python3 /usr/bin/python; 
        fi
        if [ -f /usr/libexec/platform-python ] && [ ! -f /usr/bin/python ]; then
          ln --symbolic /usr/libexec/platform-python /usr/bin/python; 
          ln --symbolic /usr/libexec/platform-python /usr/bin/python3; 
        fi
      become: true

- name: Base setup
  hosts: all
  remote_user: root
  vars:
    DockerSpawner:
      allowed_images:
        jupyter/scipy-notebook: 'jupyter/scipy-notebook:33add21fab64'
        jupyter/datascience-notebook: 'jupyter/datascience-notebook:latest'
        cschranz/gpu-jupyter: 'cschranz/gpu-jupyter:latest'
      volumes:
        /DATA: '/DATA'
        /pplasma: '/pplasma'
        /mammouth: '/mammouth'
    disable_selinux: true
    install_mainline_kernel: true
    install_container_toolkit: true
  roles:
    - lpp_machine
    - centos
    - specific_setup
    - nvidia
    - jupyterhub
    